server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }
  
  # This matches /api/trip, strips "/api", and sends /trip to the backend
  location /api/ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://backend:3000;
  }
}